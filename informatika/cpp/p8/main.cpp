#include "Employee.h"
#include "Dialog.h"
#include <iostream>

using namespace std;

int main() {
	/*Person *a = new Person;
	a->Input();
	a->Show();

	Employee *b = new Employee;
	b->Input();
	b->Show();

	Vector v(10);
	Object *p = a;
	v.Add(p);
	p = b;
	v.Add(p);
	v.Show();
	v.Del();
	cout << "Vector size: " << v();
	return 0;*/
	Dialog D;
	D.Execute();
}
