#include <cstdlib>
#include <iostream>
#include "vector.h";

using namespace std;

int main() {
	vector v(5);
	/*Object *p;

    pairr a;
    cin >> a;
    p = &a;
    v.add(p);

    rectangle b;
    cin >> b;
    p = &b;
    v.add(p);

    cout << v;*/
    return 0;
}

